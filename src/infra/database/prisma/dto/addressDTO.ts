export class AddressDTO {
  props: {
    street: string;
    city: string;
    state: string;
    postalCode: string;
    country: string;
    customerId: string;
    createdAt: Date;
    updatedAt: Date;
  };
  _id: { value: string };
}
